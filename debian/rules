#!/usr/bin/make -f

%:
	dh $@ --with phpcomposer

override_dh_phpcomposer:
	dh_phpcomposer --sourcedirectory=$(CURDIR)/debian/passbolt-api/usr/share/php/passbolt-api

override_dh_auto_clean:
override_dh_auto_build:
